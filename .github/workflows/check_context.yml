name: envvars
on:  [push]
env:
  workflowvar: ${{ github.actor }}
jobs:
  myjob:
    runs-on: ubuntu-latest
    env:
      var: Helloword
    steps:
      - name: PRINT
        run: echo $var
      - name:  access workflow var
        run: echo $workflowvar 
       
        
  
